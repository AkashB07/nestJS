export enum NodeEnvEnum {
  LOCAL = 'local',
  DEVELOPMENT = 'development',
  STAGING = 'staging',
  PRODUCTION = 'production',
}

export enum LogLevelEnum {
  INFO = 'info',
  DEBUG = 'debug',
  VERBOSE = 'verbose',
  WARN = 'warn',
  ERROR = 'error',
}
